#include <iostream>
#include <string>
#include "Graph.h"
int main() {
    Graph playlist;
    playlist.ReadFile();
    string genre;
    int playlistSize;
    double energy1, energy2;
    //if range playlist
        //call playlist.CreateRangePlaylist(genre, playlistSize, energy1, energy2);
    //if similarity playlist
        // call playlist.CreateConstantPlaylist(genre, playlistSize, energy1);
}
